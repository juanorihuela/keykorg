# Archivo raíz de comandos para macOS.
# Solo lista los archivos a importar — no define pads directamente.
# Cada archivo en macos/ agrupa pads por categoría.
#
# Para crear tu configuración real:
#   1. Copia este archivo como commands.macos.yaml
#   2. Crea tus archivos de escenario en macos/ (sin .example en el nombre)
#   3. Ajusta la lista de imports según los archivos que uses
#
# Ver ejemplos de escenarios en macos/commands_example/

imports:
  - macos/apps.yaml
  - macos/sistema.yaml
